#!/usr/bin/env bash

for file in $(ls $3); do
  var=$(cat "$3/$file")
  echo "exporting: $file -> $var"
  export "$file=$var"
done

cd $1

echo "Running command: $AFTER_DEPLOY_COMMAND"
exec $AFTER_DEPLOY_COMMAND
